# Heavy/GPU env — real generators + retrievers (Kaya, or local stand-in tests).
# Install on top of requirements-light.txt. Pin exact versions per Kaya's CUDA
# when you build the env under /group (context.md §12).
-r requirements-light.txt

# --- generation (Stage 2/3) ---
# Pin torch to the cluster's CUDA module (kaya_cheatsheet.md). colpali-engine
# also requires torch<2.12, so a 2.4–2.11 cuXXX build satisfies everything.
torch>=2.3,<2.12
torchvision                 # required by qwen-vl-utils
# Qwen2.5-VL needs >=5.3; but transformers >=5.9 RENAMED PaliGemma's submodule
# (model.language_model.model.* -> model.model.language_model.*), which silently
# breaks colpali-engine 0.3.16's LoRA load (adapter keys mismatch -> random
# weights + a full-vocab LM-loss OOM). Cap at 5.3 so ColPali's adapter applies.
transformers>=5.3,<5.4
accelerate>=0.30
qwen-vl-utils>=0.0.8        # process_vision_info
Pillow>=10.0                # real VLM image loading

# --- retrieval (Stage 4; heavy/GPU only — sparse + matplotlib are in light) ---
sentence-transformers>=3.0  # dense text
colpali-engine>=0.3         # ColPali / ColQwen2.5 visual late-interaction

# --- representation (Stage 5) ---
# mineru (magic-pdf) is a heavy optional parser; install separately if needed.
# tesseract parser uses pytesseract (in requirements-light) + the tesseract binary.
