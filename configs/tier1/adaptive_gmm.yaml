# Tier-1 #1 — adaptive top-k (ViDoRAG GMM over the score distribution).
# The corpus disagrees on the method (GMM vs k-means); run both and measure
# which wins (a contribution). Pairs with adaptive_kmeans.yaml.
name: tier1-bm25-adaptive-gmm
seed: 0
data: {name: mmlongbench-doc, slice: full}
representation: {parser: pymupdf4llm, chunking: page, dpi: 144}
retrieval:
  method: bm25
  top_k: 4
  k_strategy: gmm
  candidate_k: 16
generation:
  modality: image
  generator: kaya_vlm
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
judge: {type: rule}
