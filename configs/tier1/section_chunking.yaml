# Tier-1 #2 — flat vs structure-aware chunking (the live RAG-DocVQA vs
# MultiDocFusion/DMAP dispute). Adjudicate by comparing this section-chunked run
# against a flat chunk run (same retriever/parser): does carrying hierarchical
# heading context with each chunk help? Text retrievers only (sub-study B).
name: tier1-dense-section-chunking
seed: 0
data: {name: mmlongbench-doc, slice: full}
representation: {parser: pymupdf4llm, chunking: section, dpi: 144}
retrieval: {method: dense, top_k: 4}
generation:
  modality: text
  generator: kaya_vlm
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
judge: {type: rule}
