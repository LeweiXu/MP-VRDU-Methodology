# Tier-1 #4 — parent-section expansion (MHier-RAG / MultiDocFusion).
# After the cut, add the sibling pages of each hit's section so co-located
# figures/tables come along — tackles "the answer text and the figure are on the
# same section but split across pages". Needs a structure-aware parser
# (pymupdf4llm/mineru); degrades to no-op where the parser gives no headings.
name: tier1-bm25-expand-parent-section
seed: 0
data: {name: mmlongbench-doc, slice: full}
representation: {parser: pymupdf4llm, chunking: section, dpi: 144}
retrieval:
  method: bm25
  top_k: 3
  expand: parent_section
generation:
  modality: image
  generator: kaya_vlm
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
judge: {type: rule}
