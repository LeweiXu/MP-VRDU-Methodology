# Tier-1 #5 — adjacent-page expansion (DFVC's training-free shadow).
# After the cut, also include page i-1 and i+1 of each hit. Measures whether the
# cross-page-continuity problem (spanning tables/paragraphs) matters at all on
# the benchmark, with zero training.
name: tier1-bm25-expand-adjacent
seed: 0
data: {name: mmlongbench-doc, slice: full}
representation: {parser: pymupdf4llm, chunking: page, dpi: 144}
retrieval:
  method: bm25
  top_k: 3
  expand: adjacent
  expand_window: 1
generation:
  modality: image
  generator: kaya_vlm
  model_id: Qwen/Qwen2.5-VL-7B-Instruct
judge: {type: rule}
